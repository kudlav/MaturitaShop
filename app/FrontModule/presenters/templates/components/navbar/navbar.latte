<nav>
	<h3>{$heading}</h3>
	<ul>
		<li n:foreach="$items as $title => $link">
			<a href="
				{ifset $link[1]}{plink $link[0] $paramName => $link[1]}
				{else}{plink $link[0]}{/ifset}
			" {if 'Front:'.$link[0] == $page}class="selected"{/if}>{$title|noescape}</a>
		</li>
	</ul>
</nav>
